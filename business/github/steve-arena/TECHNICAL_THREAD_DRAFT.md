# Draft: How Steve uses Solana integrations (Arena XP thread)

Status: **draft only** — Superteam Steve Arena AGENT_ALLOWED; needs X post + SAP registry when Solana wallet path ready.
Bounty: https://superteam.fun/earn/listing/steve-agent-arena-launch-your-agent-and-win-500-usdc/
Arena: https://steve.oobeprotocol.ai/arena

## Outline (for X thread when unlocked)
1. Steve = OOBE autonomous perps agent on Solana; Arena farms XP from signed activity + content.
2. SAP MCP = agent identity/tools registry surface for Steve-compatible agents.
3. Jupiter routing = swap path for agent wallets; keep spender allowances tight.
4. Adrena perps = leveraged venues Steve can target; liquidation/risk belongs in tool policy.
5. Signer approval = never blind-sign; tool schema must show mint, amount, program id.
6. Wallet safety = separate trading hot wallet; Base USDC earn stack stays on `0xa75C…039E8`.
7. Veriton angle: HTML/JSON + probe APIs as off-chain senses; Steve stack for on-chain Solana execution.
8. CTA: register agent on SAP, farm Arena missions, submit Superteam proof pack.

## Blockers
- No aged X account for thread distribution
- No Solana receive/trading wallet in current earn rails (Base USDC primary)
- Listing pages thin via API; full submit UX needs human Superteam session

## Next
1. Solana wallet decision (research note before any SOL gas buy)
2. X unlock OR post technical article on non-X channel if Superteam accepts
3. SAP registry once wallet exists
